export interface BlockButtonProps {
    square?: boolean;
    circular?: boolean;
    borderless?: boolean;
    transparent?: boolean;
}
export interface MdiIconProps {
    iconPath: string;
    size?: number;
    inline?: boolean;
}
